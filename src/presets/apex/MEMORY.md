# Memory

Use this file for durable, long-term memory that should persist across sessions.

- Store stable user preferences, identity details, and durable decisions here.
- Use `memory/YYYY-MM-DD.md` for daily notes and short-lived working context.
- When the user says to remember something, write it down instead of only keeping it in chat context.
